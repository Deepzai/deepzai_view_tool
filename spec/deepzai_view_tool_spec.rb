require "spec_helper"

describe DeepzaiViewTool do
  it "has a version number" do
    expect(DeepzaiViewTool::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
